# Web Frontend

Place your browser client here. Suggested stack: Vite + React with a WebSocket connection to `ws://localhost:8000/ws/game`.

Dev hint:

```
npm create vite@latest web -- --template react-ts
cd web && npm i && npm run dev
```

Configure CORS/WS origin via `ALLOWED_ORIGINS` in `.env`.

